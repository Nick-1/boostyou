export enum StickerForm {
    circle = 'circle',
    rectangle = 'rectangle',
}

export enum StickerStyle {
    regular = 'regular',
    christmas = 'christmas',
}

export enum RedactorMode {
    UPDATE = 'update',
    CREATE = 'create',
}
